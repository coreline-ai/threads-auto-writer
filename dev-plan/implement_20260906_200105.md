# implement_20260906_200105.md

작성 일시: `2026-09-06 20:01:05 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

ThreadFlow OS를 먼저 localhost 웹앱으로 실행하고, 동일한 공통 UI를 Chrome Extension에서도 설치·사용할 수 있도록 런타임을 분리한다.

## 개발 범위

apps/web, packages/client-ui, apps/extension, apps/codex-gateway, 실행 스크립트와 사용자 문서

## 제외 범위

- 공개 클라우드 배포, Firefox/Safari 포팅, Meta API 자동 게시 UI, Chrome Web Store 제출

## 참조 문서

- 이전 개발 계획: dev-plan/implement_20260904_183303.md
- README.md
- docs/user-guide.md

## 과거 실패·교훈 참조

- 검색 상태: `실행 완료`
- 검색 대상 scope: `apps/web`, `packages/client-ui`, `apps/extension`, `apps/codex-gateway`
- 검색 결과: `0 match`
- 발견 시 각 항목의 `adopted | not-applicable | waived`, 이유, 예방 control, 적용 task/test 위치를 적는다.

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- material impact·재발 위험·QA escape·보안/개인정보/무결성 near-miss·큰 재작업 중 하나인 문제만 현재 Phase에 사실 기반 후보로 기록하고, 원인은 해결·검증 후 확정한다.
- 개발 종료 전 각 후보를 `plan-only | existing-reference | new-lesson` 중 하나로 분류한다.

## Phase 상태 요약

- [x] Phase 1. 공통 클라이언트와 런타임 경계 완료
- [x] Phase 2. localhost 웹 Origin 보안과 실행 환경 완료
- [x] Phase 3. 자체 웹앱 구현 완료
- [x] Phase 4. Chrome Extension 회귀와 설치 산출물 완료
- [x] Phase 5. 통합 검증과 문서화 완료

## QA 관점

- [x] 실패 케이스와 경계값을 검토한다.
- [x] 회귀 리스크와 검증 기준을 확인한다.

## 교훈 분류 요약

- [x] `OCC-SSE-CORS-001`을 `new-lesson`으로 분류했다.
- 재발 방지 문서: `docs/dev-lessons/DL-20260906T115242Z-91f4b92a.md`

---

## Phase 1. 공통 클라이언트와 런타임 경계

### 목표

- React 작성 UI·IndexedDB·Gateway Client를 재사용 가능한 패키지로 분리하고 플랫폼 동작만 Runtime Adapter로 주입한다.

### 구현 태스크

- [x] `packages/client-ui`에 App, Store, DB, Gateway Client와 CSS를 공통화한다.
- [x] Extension 전용 Chrome API를 `ClientRuntime` 경계 밖으로 분리한다.
- [x] 웹과 Extension의 캡처·외부 링크·작성창 전달 상태를 명시적 계약으로 정의한다.

### 자체 테스트

- [x] 기존 DB·Gateway single-flight·편집 이력 테스트가 공통 패키지에서도 통과한다.
- [x] TypeScript project reference와 package export를 검증한다.

### 이슈 및 수정

- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. localhost 웹 Origin 보안과 실행 환경

### 목표

- Gateway가 명시적으로 허용된 loopback 웹 Origin만 수용하고 기존 Extension Origin 보안을 유지하도록 한다.

### 구현 태스크

- [x] `THREADFLOW_WEB_ORIGINS`를 추가하고 localhost/127.0.0.1/::1 HTTP Origin만 허용한다.
- [x] 웹·Gateway 동시 실행 스크립트와 고정 포트 계약을 추가한다.
- [x] 악성·외부 웹 Origin과 경로가 붙은 Origin 설정을 거부한다.

### 자체 테스트

- [x] Origin parser 단위 테스트를 추가한다.
- [x] 웹 Origin의 health/bootstrap/auth 실제 HTTP Smoke를 수행한다.

### 이슈 및 수정

- [x] `OCC-SSE-CORS-001 | 2026-09-06 20:46 KST | Chromium이 생성 SSE를 CORS로 차단 | 웹 1턴이 Failed to fetch로 종료 | 실브라우저 콘솔과 실패 UI | raw SSE에 검증된 Origin 헤더를 복원하고 회귀 테스트 추가`
- [x] 근본 원인: `reply.hijack()` 이후 `reply.raw.writeHead()`가 Fastify CORS 플러그인의 응답 헤더 적용을 우회했다.
- [x] 예방 control: hijacked SSE 응답에 `Access-Control-Allow-Origin`·`Vary: Origin`을 직접 기록하고 테스트에서 헤더를 검증한다.
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. 자체 웹앱 구현

### 목표

- 설치 없이 localhost에서 1턴 생성·편집·승인·클립보드 전달이 가능한 독립 웹 화면을 제공한다.

### 구현 태스크

- [x] `apps/web` Vite 진입점·브랜드 Shell·반응형 레이아웃을 구현한다.
- [x] 웹 Runtime에서 캡처 제한을 정확히 표시하고 승인 글을 복사한 뒤 Threads를 연다.
- [x] 웹 OAuth 팝업 차단 시 직접 열 수 있는 안전한 fallback 링크를 제공한다.

### 자체 테스트

- [x] 웹 production build를 생성한다.
- [x] 데스크톱·좁은 화면 브라우저 렌더와 주요 컨트롤을 확인한다.

### 이슈 및 수정

- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. Chrome Extension 회귀와 설치 산출물

### 목표

- 공통 UI 전환 후에도 현재 게시물 캡처와 Threads 작성창 입력이 유지되며 설치 ZIP을 만들 수 있게 한다.

### 구현 태스크

- [x] Extension Runtime Adapter를 구현한다.
- [x] WXT manifest·background·content script 동작을 유지한다.
- [x] Extension production ZIP을 재생성한다.

### 자체 테스트

- [x] WXT Chrome MV3 build·zip을 실행한다.
- [x] Manifest와 패키지 SHA-256을 확인한다.

### 이슈 및 수정

- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 5. 통합 검증과 문서화

### 목표

- 한 명령 실행, 웹 우선 사용법, Extension 전환 방법을 문서화하고 전체 회귀를 닫는다.

### 구현 태스크

- [x] README·사용자 가이드·외부 값·구현 상태를 웹 우선 구조로 갱신한다.
- [x] 포맷·TypeScript·ESLint·Vitest·Golden eval을 수행한다.
- [x] 실제 검증 결과와 남은 외부 Gate를 기록한다.

### 자체 테스트

- [x] 전체 자동 테스트가 통과한다.
- [x] 웹과 Extension 설치 경로가 문서와 일치하는지 확인한다.

### 이슈 및 수정

- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 최종 검증 기록

- TypeScript project reference: PASS
- ESLint: PASS
- Vitest: `16 files / 85 tests` PASS
- Golden eval: `30/30` PASS
- 웹·Extension production build: PASS
- 웹·Gateway 동시 실행: `127.0.0.1:4173`, `127.0.0.1:8787` PASS
- 실제 웹 Origin CORS·Companion bootstrap·ChatGPT Pro OAuth 상태: PASS
- 실제 Chromium 1턴 생성·SSE 8단계·후보 4개·261/500 최종 Draft: PASS
- 최종 승인 저장·보관함 v1·새로고침 복구: PASS
- Chromium 1280px·390px 시각 QA: PASS
- Extension ZIP SHA-256: `bcf987ee5ce1ef0f47bfaff95dfedb32c6773b0d3c7b3e320b2284285385ebb6`
