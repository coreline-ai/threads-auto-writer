# implement_20260906_225905.md

작성 일시: `2026-09-06 22:59:05 KST`

상태: **구현 완료 — 2026-09-07**. 자동·실브라우저 검증을 완료했으며, 문서 끝의 실기·승인 필요 수동 Gate는 별도로 남긴다.

## 개발 목적

에디토리얼 스튜디오의 반복 입력·스크롤·초안 유실 위험을 줄인다. 다크 테마 기반을 먼저 정리하고, 직전 제안 중 체감 효과가 높은 편의 기능 네 가지를 순서대로 구현한다.

## 개발 범위

- 1차: 시스템/라이트/다크 테마, 화면 내 수정 요청과 적용 전 비교, 고정 작업 바, 새 작업과 임시 초안, 보관함 검색·상태 필터.
- 공통 React UI·웹/Extension 진입점·로컬 저장소 및 해당 회귀 테스트.
- 수정 미리보기를 안전하게 제공하는 데 필요한 Gateway revision 계약의 제한적 보완. 새 LLM 공급자나 외부 서비스는 추가하지 않는다.
- README·사용자 가이드·구현 상태·설치물 갱신은 실제 구현 완료 후 수행한다.

## 제외 범위

- Git 커밋·푸시. (문서 전용 제한은 후속 구현 요청으로 해제했다.)
- 자동 게시 확대, 공개 배포, 클라우드 동기화, 통계 대시보드, 새 LLM/인증 체계, 검수 기준 완화.
- 2차 후보인 작성 설정 프리셋·오류별 복구 버튼·생성 단축키. 아래 후속 목록에만 남기고 1차 범위를 자동 확장하지 않는다.

## 참조 문서

- [README](../README.md)
- [현재 디자인 명세](../docs/editorial-studio-design.md)
- [다크 테마 구현 제안](../docs/dark-theme-proposal.md)
- [직전 리디자인 개발 계획](implement_20260906_214210.md)
- [잔여 검증 사항](../docs/remaining-work.md)

## 과거 실패·교훈 참조

- 검색 상태: 실행 완료. scope: `packages/client-ui`, `apps/web`, `apps/extension`, `apps/codex-gateway`, `packages/contracts`; 태그 필터 없음.
- 검색 결과: 1 match, `DL-20260906T115242Z-91f4b92a` → **adopted**.
- 예방 control: 기존 검증된 SSE CORS 헤더를 유지하고 서버 테스트만으로 브라우저 성공을 판단하지 않는다.
- 적용 위치: Phase 2 Gateway 계약 회귀, Phase 6 실제 웹 Origin 생성/SSE/수정 미리보기 검증.
- 직전 계획의 LLM 출력 검증 실패는 원인이 미확정이다. 실패 입력 보존 테스트로 참고하며 자동 복구 완료나 원인 해결을 주장하지 않는다.

## 공통 진행 규칙

- 각 Phase의 자체 테스트 완료 후에만 다음 Phase를 시작한다.
- 기존 미커밋 변경과 승인 Draft 이력은 보존한다. DB 초기화로 마이그레이션을 대체하지 않는다.
- 기존 React·Dexie·Zustand·Gateway와 표준 브라우저 API를 우선 사용한다. 불필요한 의존성·추상화는 추가하지 않는다.
- 테마 변경·미리보기 생성·작업 전환으로 승인 상태를 새로 부여하지 않는다. 적용/편집 후 기존 승인 본문과 다르면 전달을 차단한다.
- 비동기 결과는 요청 시점의 작업 ID·기준 본문/편집 revision에 묶는다. 늦은 응답으로 다른 작업이나 최신 편집을 덮어쓰지 않는다.
- material 이슈만 해당 Phase에 시각·증상·영향·증거·임시 조치로 기록한다. 원인 검증 후 `plan-only | existing-reference | new-lesson`로 분류한다.
- 테마 제안과 계획의 검증은 구현 완료 검증과 별개다. 현재 94개 통과 기록은 이전 구현의 기준선이지 새 기능 합격 증거가 아니다.

## 현행 코드에서 확인한 제약

| 현행                                                                  | 계획에 반영할 내용                                                 |
| --------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 공통 CSS에 색상 리터럴 42회/34종, 웹 CSS에 1회                        | 배경뿐 아니라 버튼·입력·위험·Diff·포커스까지 의미별 토큰으로 전환  |
| 수정 요청은 `window.prompt`, 결과는 즉시 `workflow.edit`              | 화면 내 요청·미리보기·적용/버리기를 분리                           |
| Gateway 수정은 `job.result.finalDraft.text` 기준이며 서버 결과를 갱신 | 현재 편집본 전달과 서버 최종 결과를 바꾸지 않는 preview 경로 필요  |
| `workspaceStates`는 `id: current` 하나, DB version 3                  | 여러 작업 저장을 위한 명시적 migration과 저장 후 전환 필요         |
| 보관함은 최근 승인 버전 30개만 표시                                   | 검색은 제한 전에 전체 저장 레코드에 적용, 버전 중복 표시 정책 필요 |

## 예상 변경 파일 / 책임

신규 파일명은 구현 시 조정할 수 있지만 아래 책임 경계는 유지한다.

| 파일/영역                                                                               | 최소 책임                                                     |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| `packages/client-ui/src/appearance.ts` + `ThemeControl.tsx` (신규)                      | 테마 모드 결정·저장·구독과 선택 UI                            |
| 공통 초기화 asset 및 두 앱의 HTML/진입점                                                | React 렌더 전 테마 초기화, Extension CSP 준수                 |
| `packages/client-ui/src/style.css`, `apps/web/src/web.css`                              | 공통 색상 토큰·고정 작업 바·반응형                            |
| `RevisionPanel.tsx` (신규), `App.tsx`, `workflow-store.ts`                              | 수정 요청·비교·적용 상태와 편집 이력                          |
| `packages/contracts/src`, `gateway-client.ts`, Gateway `app.ts`/`generation-manager.ts` | 현재 본문 기반 수정 preview 계약·검증·구버전 호환             |
| `EditorActions.tsx` (신규)                                                              | 승인/전달 조건과 클릭 처리의 단일 UI; 중복 푸터 금지          |
| `database.ts`, `draft-workspaces.ts` (신규)                                             | 작업 snapshot 저장·복구·전환·검색, migration/Export/전체 삭제 |
| `apps/web/test`, Extension 저장소 테스트, Gateway 테스트                                | UI·저장·계약·동시성·테마 회귀                                 |

## Phase 상태 요약

- [x] Phase 1. 테마 기반과 다크 모드
- [x] Phase 2. 화면 내 수정 요청·비교·적용
- [x] Phase 3. 고정 작업 바 (코드/단위검증; 화면 종합 검증은 Phase 6)
- [x] Phase 4. 새 작업·임시 초안 보존
- [x] Phase 5. 보관함 검색·상태 필터
- [x] Phase 6. 통합 QA·문서·설치물

## QA 관점

- [x] 저장 실패·읽기 실패·테마 저장 차단·손상된 설정에서도 본문이 유실되지 않는다.
- [x] 미리보기 버리기/실패/늦은 응답이 현재 본문·승인 snapshot을 바꾸지 않는다.
- [x] 작업 전환과 250ms 저장 지연, 여러 탭의 같은 작업 수정 경쟁을 검증한다.
- [x] 모든 테마에서 경고·활성/비활성·포커스를 색만으로 구분하지 않는다.
- [x] CSS 200% 확대·긴 경고·키보드 Tab에서 작업 바가 클릭/포커스를 가리지 않음을 확인. 실기 키보드는 별도 미검증.
- [x] 31번째 이후 글·과거 버전 검색 누락, 승인본/작업본 중복과 오래된 승인 배지를 검증한다.
- [x] 웹/Extension Origin별 저장 분리와 미승인 전달 차단을 유지한다.

## 교훈 분류 요약

- 계획 작성 시 신규 발생 후보 없음. 구현 중 후보가 생기면 해당 Phase에 기록한다.
- [x] OCC-01 Provider 오류는 원인 미확정 plan-only, OCC-02 클릭 가림은 신규 Lesson. 기존 SSE CORS 교훈 유지. 문서 validate PASS.

## Phase 1. 테마 기반과 다크 모드

### 목표 / 구현 태스크

- [x] `system | light | dark` 선호와 실제 적용 색상을 분리한다. 미설정 기본값은 시스템 모드다.
- [x] [다크 테마 제안](../docs/dark-theme-proposal.md)의 의미별 토큰으로 하드코딩 색을 교체한다. 기존 라이트 레이아웃은 유지한다.
- [x] 상단 테마 메뉴와 설정의 명시적 선택을 같은 저장 원천에 연결한다. 선택 실패를 저장 완료로 표시하지 않는다.
- [x] 첫 화면 깜박임 방지용 공통 외부 초기화 asset을 두 앱에 연결한다. 테마 설정만 저장하고 인증 데이터와 섞지 않는다.
- [x] 테마 설정의 Export/전체 삭제 동작을 연결한다. 이 단계에는 글 저장 DB migration이 필요하지 않다.

### 자체 테스트

- [x] 시스템 변경·수동 선택 우선순위·같은 Origin 다른 탭·재실행·저장 차단·손상 값 테스트.
- [x] 라이트/다크의 본문·버튼·경고·Diff·포커스 대비와 340/390/1024/1440px 화면 확인.
- [x] 웹/Extension production 초기 HTML/asset 순서·CSP·첫 paint 확인. DB hydrate 지연에도 화면 반전이 없다.

### 이슈 및 수정

테마 6개 + 기존 UI 9개 회귀 PASS, 두 앱 build/외부 bootstrap 순서 확인. Chromium 두 테마 4개 폭·복원 PASS. 상태별 종합 캡처와 첫 paint 지연 주입은 Phase 6에서 추가 확인한다.

### 완료 조건

- [x] 구현·자체 테스트 완료, Phase 2 진행 가능.

## Phase 2. 화면 내 수정 요청·비교·적용

### 목표 / 구현 태스크

- [x] 팝업 대신 편집기 안에 요청 입력·기존 수정 범위 선택·요청 중 상태를 제공한다. 간단한 요청 예시는 피드백 문자열로 처리한다.
- [x] Gateway 계약에 검증된 `baseText`와 preview 모드를 추가하고 기존 호출의 호환성을 유지한다. preview에서는 서버의 최종 Draft 결과를 갱신하지 않는다.
- [x] 현재 편집본을 기준으로 수정하고 원본 요청의 근거/위험 검사 조건을 유지한다. preview 생성 자체는 승인이나 본문 변경이 아니다.
- [x] 수정 전후 비교 후 `적용 / 버리기`. 적용은 편집 이력 한 단계로 처리하고 재검수·재승인 조건을 평가한다.
- [x] 중복 요청 방지, 기준 본문/작업 불일치 결과의 적용 차단, 실패 시 요청 내용 보존. Gateway 작업 문맥 만료는 설명하고 새 생성이 필요함을 알린다.

### 자체 테스트

- [x] 수동 편집한 본문이 수정 기준으로 전달되고, preview 버리기 후 다음 수정도 현재 본문에서 시작한다.
- [x] 응답 전 추가 편집·요청 경쟁·작업 문맥 만료·잘못된 본문/출력·timeout에서 덮어쓰기 없음.
- [x] 적용 전 승인 유지/적용 후 재승인·undo/redo·기존 revision 호출 호환·Gateway Origin/CORS 회귀 통과.

### 이슈 및 수정

미리보기 4개 + Gateway 4개 + 기존 UI 9개 PASS. 현재 본문 전달·preview 서버 결과 불변·중복 요청 409·invalid output 502·stale 적용 차단 확인.

### 완료 조건

- [x] 구현·자체 테스트 완료, Phase 3 진행 가능.

## Phase 3. 고정 작업 바

### 목표 / 구현 태스크

- [x] 글자 수·검수 요약·승인·전달을 하나의 `EditorActions`에 모은다. 기존 본문 경고 상세는 유지한다.
- [x] 데스크톱은 편집 영역 하단 sticky, 좁은 화면은 결과 작업 영역 안에서 접근 가능한 하단 동작을 제공한다.
- [x] 하단 여유 공간·safe area·키보드 가림을 처리하고, 버튼을 두 군데 복제하지 않는다.

### 자체 테스트

- [x] 0/500/501자, 긴 경고, 승인/수정/저장 중 상태와 전달 차단 회귀.
- [x] Phase 6에서 네 가지 폭·CSS 확대·실제 클릭/Tab 검증 PASS. 모바일 소프트 키보드·브라우저 자체 확대는 아래 미검증 Gate로 분리.
- [x] 라이트/다크 및 Extension 번들 340/520px 통합 캡처 PASS (Chrome API 모의).

### 이슈 및 수정

컴포넌트 경계값 + 기존 UI 10개 PASS, typecheck PASS. 중복 전달·승인 요청 잠금과 승인 저장 대기 중 편집 보존을 추가했다. 화면 캡처는 최종 UI가 완성되는 Phase 6 통합 QA로 이동; 실기 키보드는 미검증.

### 완료 조건

- [x] 구현·자체 테스트 완료, Phase 4 진행 가능.

## Phase 4. 새 작업·임시 초안 보존

### 목표 / 구현 태스크

- [x] DB v4에 별도 `workingDrafts` 저장소와 활성 작업 ID를 추가한다. v3의 `current` snapshot을 transaction으로 이관하며 원본 승인 버전은 유지한다.
- [x] 작업별 입력·문체·근거·후보·본문·이력·검수 기준·승인본 연결을 보관한다. 활성 작업을 별도 중복 원천으로 저장하지 않는다.
- [x] `새 작업`은 현재 작업의 지연 저장을 flush한 뒤 전환한다. 저장 실패 시 전환하지 않고 현재 본문을 유지한다.
- [x] 새 작업의 문체·독자 설정은 유지하되 이전 원문·URL·근거·결과·승인·이미지 메타데이터는 초기화한다.
- [x] 생성/수정 요청 진행 중 전환은 제한한다. 작업 ID와 편집 revision을 검증해 늦은 응답/저장이 다른 작업에 적용되지 않게 한다.
- [x] 같은 작업의 다중 탭 저장 충돌은 revision 비교로 감지하고 무조건 덮어쓰지 않는다. Export/전체 삭제 transaction에 신규 테이블을 포함한다.

### 자체 테스트

- [x] v3→v4 이관, 빈 DB, 손상 snapshot, 이관 실패/재열기, 기존 승인 버전/연결 키 보존.
- [x] 생성 전/생성 후/승인 후 새 작업, 저장 지연 중 전환, quota 오류, 새로고침, 다른 탭 충돌 회귀.
- [x] 초안 간 본문·근거·승인 섞임 없음. Export에 새 작업 포함, 연결 키 제외, 전체 삭제 후 잔여 작업 없음.

### 이슈 및 수정

DB v4 migration/rollback/손상 복구/충돌/queue/quota/export/delete 9개와 UI 12개 PASS. 지연 저장 flush 실패 시 전환 중지, 전환 중 입력 잠금, 새 작업 전 근거·승인·이미지 초기화 확인. 실제 브라우저 복원은 Phase 6에서 재확인한다.

### 완료 조건

- [x] 구현·자체 테스트 완료, Phase 5 진행 가능.

## Phase 5. 보관함 검색·상태 필터

### 목표 / 구현 태스크

- [x] 검색과 `전체 / 작성 중 / 승인 완료` 필터를 추가한다. 첫 문장 기반 제목과 최근 수정 시각으로 표시한다.
- [x] 전체 로컬 작업·승인 버전의 본문을 검색한 뒤 결과를 제한/페이지화한다. 현재 30개를 먼저 잘라 검색하지 않는다.
- [x] 평소에는 작업/초안별 최신 항목으로 묶고, 이전 버전에만 검색어가 있으면 해당 버전 일치임을 표시한다.
- [x] 상태는 저장된 승인본과 현재 작업 본문 일치를 기준으로 계산한다. 수정된 작업에 과거 승인 배지를 붙이지 않는다.
- [x] 기존 버전 비교·게시 결과 연결·작업 복구를 유지한다. 외부 검색/벡터 DB는 추가하지 않는다.

### 자체 테스트

- [x] 0/1/31/200개 레코드, 같은 Draft 여러 버전, 과거 버전에만 일치하는 본문 검색.
- [x] 한글·공백·이모지·빈 검색·검색 후 편집/승인 상태 변경과 결과 페이지 경계.
- [x] 검색 결과 열기 후 요청/검수 기준 복원, 두 테마·좁은 폭 렌더 회귀.

### 이슈 및 수정

로컬 전체 검색 6개 + UI 12개 + 수정 패널 4개 PASS. 최신 작업별 표시, 승인본 중복 억제, 과거 버전 검색 표시, 20개 페이지 처리 구현. 실제 좁은 폭은 Phase 6에서 캡처. 검색은 문자열 기준으로 의미 검색이 아니다.

### 완료 조건

- [x] 구현·자체 테스트 완료, Phase 6 진행 가능.

## Phase 6. 통합 QA·문서·설치물

### 목표 / 구현 태스크

- [x] 테마 전환→생성→수정 preview→적용→임시 보관→새 작업→검색→승인→복원의 통합 흐름 검증.
- [x] 타입·린트·포맷·전체 테스트·Golden·웹/Extension 빌드·ZIP 및 변경된 Gateway Companion 재패키징.
- [x] 사용자 가이드·구현 상태·migration 주의사항·최신 설치물 hash를 실제 결과로 갱신한다.

### 자체 테스트

- [x] 실제 웹 Origin의 Codex 생성/SSE와 revision preview·실패 복구 검증. 테스트 키·계정 정보가 로그/이미지에 포함되지 않는다.
- [x] 실제 연결 테스트와 모의 Chrome API 레이아웃 검증을 구분하고, 네이티브 Extension/키보드 등 미검증 항목은 명시한다.
- [x] 모든 기존 자동 테스트 유지 및 새 회귀 통과. 실제 Threads 전달/게시 없이 승인 조건을 검증한다.

### 이슈 및 수정

- OCC-01 (2026-09-07 실브라우저 생성): Provider 연결 오류 1회. 입력 보존 확인 후 한 차례 재시도 성공. 내부 원인 미확정, 재시도 성공을 상시 안정성으로 해석하지 않음. 분류: plan-only.
- OCC-02 (2026-09-07 06시대 시각 QA): bottom sticky 바가 펼친 Diff와 확대 화면의 summary 클릭을 가림. 200% CSS 확대에서 버튼 오른쪽 9px 넘침도 확인. 이미지와 pointer intercept 오류로 재현, 비교/입력/좁은 편집 영역은 일반 하단 배치, grid minmax(0, …)로 보완. 두 테마·4개 폭·실제 클릭/Tab·CSS 200% 재검증 PASS. 분류: new-lesson → DL-20260906T214830Z-a3d3a723. scripts/qa/editor-actions-layout.js로 재발 방지; find 0건 후 신규 기록, validate PASS.
- Companion 패키징은 sandbox DNS 제한으로 최초 실패, 승인된 재실행으로 성공. 앱 의존성 추가나 키 변경 없음. 분류: plan-only.

### 완료 조건

- [x] 검증 결과·발생 후보 분류·잔여 위험 기록 후 개발 완료.

### 최종 검증 요약

125/125 테스트, Golden 30/30, 타입·린트·포맷·웹/Extension build·ZIP·Companion 패키지 PASS. 실제 Codex 생성→preview 2회→버리기/적용→undo/redo→승인→새 작업→검색→복원 PASS. 두 실제 탭 충돌·내보내기에서 승자 저장본과 패자 currentRecovery 보존·연결 키 제외 확인. 앱 JS 지연 상태에서 웹/Extension 저장 테마 선행 적용, 두 테마 네 화면·폭·CSS 200%·포인터·Tab 검증 완료.

[상세 검토](../docs/convenience-theme-review.md), [설치물 hash](../docs/release-readiness.md).

## 별도 수동 Gate — 구현 완료와 구분

- [ ] 실제 모바일 소프트 키보드·브라우저 자체 확대.
- [ ] 네이티브 Extension 설치/닫기·재열기와 실제 Threads 로그인·작성창 전달.
- [ ] 실브라우저 전체 삭제 버튼: 보안 승인에서 데이터 삭제 위험으로 차단. 사용자에게 테스트 전용 프로필 실행 승인을 요청했으며, DB/테마 초기화 자동 테스트는 통과했다.
- [ ] 신규 Mac 설치·사람에 의한 글 품질 평가.

## 후속 후보 — 1차 구현 제외

| 후보                | 착수 시 필요한 범위                                                    |
| ------------------- | ---------------------------------------------------------------------- |
| 내 작성 설정 프리셋 | 문체/독자/목적 저장·선택, 현재 작업 적용 전 내용 확인                  |
| 오류별 복구 버튼    | 오류 분류에 맞는 사용자 선택 재시도, 중복 실행·무한 자동 재시도 방지   |
| 작성 단축키         | IME 조합 입력·브라우저 기본 단축키 충돌 확인, 승인/전달 자동 실행 금지 |

## 계획 작성 검증

- [x] 현행 코드·DB 버전·revision 입력과 서버 부작용을 확인했다.
- [x] 참조 Lesson 검색·적용 위치·Phase별 자체 테스트를 작성했다.
- [x] 다크 색상 후보의 단색 대비를 계산했다. 실제 렌더 검증은 Phase 1에 남겼다.
- [x] Markdown 포맷·문서 링크·변경 범위를 확인했다. 앱 코드·데이터는 변경하지 않았다.
